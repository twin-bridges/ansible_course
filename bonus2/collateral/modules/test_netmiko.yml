---
- name: Netmiko module
  hosts: cisco
  gather_facts: False
  tasks:
    - name: Netmiko testing
      netmiko_send_command:
        host: "{{ ansible_host }}"
        device_type: cisco_ios
        username: pyclass
        password: invalid
        command: show ip int brief
